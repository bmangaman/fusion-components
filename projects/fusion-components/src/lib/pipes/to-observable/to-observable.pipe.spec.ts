import { ToObservablelPipe } from './to-observable.pipe';

describe('GetTableStatePipe', () => {
  let pipe: ToObservablelPipe;

  beforeEach(() => {
    pipe = new ToObservablelPipe();
  });

  describe('transform()', () => {
    // TODO
    xit('should return the provided value as an observable', () => {});
  });
});
