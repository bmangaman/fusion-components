import { ModalContentComponent } from './modal-content.component';

describe('ModalContentComponent', () => {
  let component: ModalContentComponent;

  beforeEach(() => {
    component = new ModalContentComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
